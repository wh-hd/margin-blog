# 2026-07-19

## 用户确认 /admin/ 安全模型
- 用户询问 `/admin/` 是否只能由验证用户访问。
- 说明：/admin/ 静态登录页本身公开，但 GitHub OAuth 授权 + `repo` scope 后才能真正读写仓库；OAuth App 绑定 wh-hd，实际只有用户可发布。
- 用户若需更严格保护，可追加 Cloudflare Access 或 HTTP Basic Auth。
