# 2026-07-18 工作记录

- 继续个人博客正式部署。用户上传本地录屏（E:/works/个人博客/参考视频/75a471f3e2cf0f3629de43b2b22e9001.mp4）后，通过 FFmpeg 抽帧 + 直接读图确认视频推荐的生产部署方式是 **Cloudflare Workers & Pages**（创建项目 → 连接 GitHub/上传静态文件 → 部署 → 可选绑定自定义域名）。用户确认严格按视频走 Cloudflare Pages。
- 已完成本地准备：重新构建 dist/（19 页面 + 1 重定向 + 契约校验通过）、初始化 Git 仓库（main 分支，初始提交 ea14263）、更新 .gitignore。
- GitHub 仓库已建：https://github.com/wh-hd/margin-blog 。本机 SSH 密钥 id_rsa 已授权该账号，已通过 SSH 推送 main 分支成功（无需令牌）。
- 下一步：用户在 Cloudflare 登录并授权 GitHub 后，于 Cloudflare Pages 创建项目连接该仓库，设置 Framework preset=Astro / Build command=npm run build / Build output directory=dist，部署取得 *.pages.dev 地址。域名暂不绑定（按之前决策，待后续购买主域再配）。
- 已知构建注意点：Cloudflare Pages 需 Node 22+，仓库已有 .nvmrc=22.22.2 可满足；构建脚本含 node scripts 后处理与 npm ci，需 package-lock.json（已具备）。
- 完成收尾：将 astro.config.mjs 的 site 与 src/content/site/config.json 的 siteUrl 由 example.com 改为 https://margin-blog.pages.dev；本地质量门禁全绿（lint 0 warning、20 单元测试通过、build 19 页 + 契约校验通过）；提交 e65d31f 并推送 main，Cloudflare 自动重建部署成功。核验线上首页 canonical/og=8 处、sitemap=18 处、robots 均已变为 margin-blog.pages.dev，HTTP 200。**测试部署正式可用**。
- 用户新需求（后续）：要做一个“真实的关于我个人的博客”，且希望内容修改能“比较方便地自己上传/编辑”。需在真实博客方案中考虑低门槛内容编辑方案（候选：Decap CMS 等 git-based 编辑器 + Cloudflare Workers OAuth 代理；或保留 Markdown 内容集合 + 简易提交流程）。等用户确认编辑方式后再动工。
- 用户已确认：**Decap CMS + 现在就搭骨架**。已在本仓库落地一套可自助编辑方案（未另建仓库，沿用已连 GitHub/Cloudflare 的 margin-blog）：
  - 新增 `public/admin/index.html`（CDN 加载 decap-cms@^3）+ `public/admin/config.yml`：github 后端，repo=wh-hd/margin-blog、base_url/site_url=https://margin-blog.pages.dev、auth_endpoint=/oauth、publish_mode=simple；collections 含 posts（标题/摘要/发布日期/分类/标签/草稿/精选/正文，字段约束对齐 zod schema 防构建失败）与 site 文件集合（编辑 src/content/site/config.json 的 姓名/品牌/关于我/联系/当前关注/社交等，可自助改“关于我”）。为防 Decap 把可选字段写成空串导致 zod 校验失败、部署卡住，刻意省略了 canonical/updatedDate/cover/evidenceUrl/timelineNotice/contactBoundary 等可选/高级字段（需手动改）。
  - 新增 `functions/oauth.ts`（Cloudflare Pages Function，非独立 Worker）：单端点处理 GitHub OAuth 两步——首次 302 到 github authorize，回调带 code 时换 token 并 302 回 /admin/#access_token。读 GITHUB_CLIENT_ID / GITHUB_CLIENT_SECRET（Cloudflare Pages 环境变量）。因 @typescript-eslint/no-explicit-any，给该文件加了文件级 eslint-disable，避免为类型去装 @cloudflare/workers-types（遵守“装依赖先问”）。
  - 质量门禁：lint 0 warning（修后）、20 单元测试通过、build 19 页 + 契约校验通过；robots.txt 增加 `Disallow: /admin/`。提交 c7e380b 并推送 main，Cloudflare 自动重建：线上 /admin/ 返回 200、/oauth 返回 302 到 github authorize（client_id=undefined 属正常，因环境变量未设）。
  - **待用户完成的账号步骤（决定登录是否可用）**：① 在 https://github.com/settings/developers 新建 OAuth App（Homepage=https://margin-blog.pages.dev，Authorization callback URL=https://margin-blog.pages.dev/oauth），拿到 Client ID + 生成 Client Secret；② 在 Cloudflare Pages 项目 margin-blog 的 Settings → Environment variables（Production）新增 `GITHUB_CLIENT_ID` 与 `GITHUB_CLIENT_SECRET`，保存后建议再触发一次部署绑定变量。完成后访问 /admin/ 点登录即自助发文/改“关于我”，保存即自动重部署。
  - 后续若绑自定义域名：需同步改 OAuth App 的 callback、Decap config.yml 的 base_url/site_url、functions/oauth.ts 的 SITE_URL，并重新生成域名对应的 Client Secret/回调。
- 用户已创建 OAuth App（wh-blog，Client ID=Ov23liGxD4Y6j3PN1IfN）并把 Client ID / Secret 填到 Cloudflare Production Variables。首次登录仍报 `incorrect_client_credentials`：排查发现 Cloudflare 里变量名写成了 `GITHUB_CLIENT_SECRE`（少了末尾 T），函数读 `GITHUB_CLIENT_SECRET` 因此拿不到 Secret。用户把名字修正后，/admin/ 登录成功交换到 GitHub token（URL 出现 `access_token=gho_...`），但 Decap 没实际登录成功——因为 `/oauth` 回调用了 URL hash 回传 token，而 Decap 的 GitHub 后端要求 OAuth 代理通过 `window.opener.postMessage` 把 token 送回父窗口并关闭弹窗。
- 修复 `functions/oauth.ts`：回调成功后返回一段 HTML，内嵌脚本执行 `window.opener.postMessage(JSON.stringify({token, provider: "github"}), "*")` 再 `window.close()`；移除不再需要的 adminRedirect 与 hash 重定向。lint 0 warning、20 测试通过、build 19 页 + 契约校验通过。提交 82c8ef2 并推送 main，等 Cloudflare 自动重建后 /admin/ 登录应可正常进入 Decap 后台。
- 用户再次测试：授权后 Decap 仍没登录成功。排查发现 Decap CMS 的 GitHub 后端需要 OAuth 握手：弹窗先向父窗口发 `authorizing:github`，父窗口回应后再回传 token。我的第一次修复直接发 token，缺少握手，导致 Decap 没处理。修复 `functions/oauth.ts`：在成功回调 HTML 中 `window.opener.postMessage("authorizing:github", "*")` 并监听同消息，收到后回传 token 并关闭窗口。提交 a96a5fa 并推送，待部署后验证。
- 用户要求：不在网站显示真实姓名（温辉），统一用 "wh" 作为个人标识；不直接出现"参考 Sac / sac-ai.com"等外部参考来源；参考 sac-ai.com 是学其**个人博客设计实现**，不是抄写内容。已清理：① 站点配置 name/brand/intro/description/currentFocus 全部从"温辉"改为"wh"，并移除 sac-ai.com 参考表述；② 5 篇文章中的 `> 参考来源：[Sac 的 sac-ai.com]` 和 `> 原文链接：[X @Saccc_c]` 全部替换为"内容待完善"占位提示；③ 全仓库 grep 确认已无 温辉/Sac/sac-ai/Saccc_c 残留。GitHub 链接保持为 https://github.com/wh-hd。质量门禁全绿（lint 0 warning、20 测试通过、build 18 页 + 契约校验通过）。提交 2df5f3b 并推送 main，Cloudflare 自动重建后线上首页出现 14 处 wh、关于页 13 处 wh，无真实姓名与外部参考残留，HTTP 200。后续用户可在 /admin/ 里逐步替换占位内容为真实个人实践。
- 全站设计升级（参考 https://github.com/donghaoi/AyeezBlog 的"酷炫"风格，但按用户选择落地为**浅色精致升级 + 保留粒子场鼠标联动**）：① 文章列表从行式改为响应式**卡片流**（ArticleRow→card，hover 上移+阴影，ArticleList→auto-fill 网格）；② 文章详情页新增 **TOC 目录侧栏**（用 render() 的 headings，IntersectionObserver 滚动高亮）、**悬浮工具按钮**（回顶 + 目录抽屉切换，移动端 TOC 变抽屉）、**代码块一键复制**（client script 给 .prose pre 包 wrapper+复制按钮）；③ 新增 **/archive/ 时间轴归档页**（view-models 加 getArchivePageModel，按年月分组，types 加 ArchivePageModel/ArchiveGroup）；④ Hero 品牌名改为由 site.brand（wh）驱动、去掉硬编码"侧注 MARGIN"，Header 新增"归档"导航项；⑤ 保留 AiParticleField 的颗粒感(noise)+鼠标联动。质量门禁全绿（lint 0 warning、20 测试通过、build 19 页含新 /archive/ + 契约校验通过）。提交 7ffe33a 并推送 main，Cloudflare 自动重建后线上核验：/archive/ 200 且含 archive-group/archive-item，/articles/ 含 article-card，文章详情含 data-toc/floating-tools，首页 h1 渲染为 `wh`，导航含 /archive/ 归档。
- **用户指出关键差距不是配色而是功能缺失**：AyeezBlog 有 GitHub 外链、公告卡片、友链、说说、日志、管理端入口等模块，我们之前都没做（GitHub 链接虽配在 config.social 但导航/页脚根本没渲染）。本次补齐：
  - **导航/页脚渲染 social 外链**：SiteHeader 从 site.social 渲染 GitHub 等图标链接（inline SVG，含 GitHub/X/通用三态），并加 `/admin/` 管理端入口；SiteFooter 加社交链接区。`BaseLayout` 传给两者 `social={site?.social}`。
  - **首页公告卡片**：site config 加 `announcement` 字段（text/link/linkLabel，站内路径 regex 校验），首页在 Hero 后渲染公告 band（site.announcement 从 model 透传）；Decap site 集合加 announcement 对象字段。
  - **新增 links/says/logs 三个内容集合**（content.config.ts 加 glob loader；types.ts 加 LinkItem/LinksPageModel/SaysPageModel/LogsPageModel + SiteSocialLink/SiteAnnouncement；queries.ts 加 getLinks/getPublishedSayEntries/getPublishedLogEntries；view-models.ts 加 getLinksPageModel/getSaysPageModel/getLogsPageModel）。**注意 Astro 5 content layer 渲染正文必须用 `import { render } from 'astro:content'` + `await render(entry)`，不能用 `entry.render()`（会报 render is not a function）**。
  - **新增三个页面**：`/links/`（友链卡片网格，读取 LinkItem）、`/says/`（说说流，每条用 render(entry).Content 渲染 Markdown 正文 + mood 标签）、`/logs/`（日志列表，含 category 标签）。导航与页脚均加入口。均带占位示例数据（标注"内容待完善/示例"，用户后续在 Decap 替换）。
  - **Decap config.yml** 加 links/says/logs 三个 folder 集合 + site.announcement 字段。
  - 质量门禁全绿（lint 0 warning、20 测试通过、build **22 页**含 /links//says//logs/ + 契约校验通过）。提交 a57f7ae 并推送 main，Cloudflare 自动重建后线上核验：/links//says//logs/ 均 200；首页 grep 命中 `github.com/wh-hd`(×4)、`announcement-tag`、`站点已升级`、`/links/`、`/says/`、`/logs/`、`/admin/` 导航入口全部就位。**功能模块已对齐参考站**。
  - 遗留：留言/评论（Giscus 等第三方）未做，因静态站需外部服务；相册未做（可后续加 gallery 集合）。这两项是用户后续可选增强。
