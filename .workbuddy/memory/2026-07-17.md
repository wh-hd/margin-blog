# 2026-07-17

- 完成当前 Astro 个人博客实现梳理：项目为静态 Markdown-first 博客，采用 Content Collections/Zod、页面 View Model、SEO/JSON-LD、分类标签、alias 301、Cloudflare Assets 部署及 Vitest/构建契约校验。
- 当前发布阻断点：缺少根目录 npm lockfile但 CI 使用 npm ci；依赖均为 latest；域名、作者与联系方式仍为占位；需在正式上线前完成真实构建和 Cloudflare 行为验证。
- 用户希望称呼为“红豆”；默认协作风格为务实、直接、清晰的工程搭档风格。
- 完成工作空间迁移一致性只读审计：源码、配置、脚本、测试和项目记忆中未发现旧绝对路径，剪切后相对路径基本正常；确认自动安装/初始化未收尾，当前无 Git 元数据、无 `.gitignore`、无 lockfile，`node_modules` 存在 257 个随机后缀临时目录且关键包缺文件，lint/type-check/test 均因此无法启动；CI Node 20.18 与当前 Astro 7/Wrangler 4 的 Node >=22 要求冲突。
- 已统一 Node 22.22.2、npm 10.9.7、精确依赖版本、package-lock 与 CI；隔离损坏依赖和污染 lockfile。增强未来文章/草稿隔离、联系信息状态、JSON-LD 与 sitemap 规则，新增相关测试。
- 最终验证：ESLint 通过，Vitest 8 个文件 20 项测试通过，Astro 静态构建生成 19 个页面，构建契约验证 2 个未发布排除项与 1 个 sitemap。移除已弃用且安装不完整的 lucide-astro，避免生产构建失败。正式上线仍需域名、作者名、联系策略、真实内容与 OG 图。
- 首页视觉升级：参考高密度颗粒视觉语言，但采用原创的“AI 语义向量场”概念，新增纯 SVG/CSS 微粒、连接轨迹、噪声纹理与冷青色光晕；Hero 改为左侧内容主张、右侧 AI 粒子场和悬浮重点文章卡，并适配移动端与 reduced-motion。
- AI 粒子场已增加鼠标联动：邻近粒子按距离柔和排斥、整体反向视差、局部指针光晕、离开后弹性归位；交互使用 requestAnimationFrame，仅桌面精细指针启用，触控和 reduced-motion 自动禁用。
- 完成正式上线路线调研与决策：当前为纯静态 Astro 站，无需购买 VPS；先沿用现有 Wrangler 配置部署 Cloudflare Workers Static Assets 免费地址，验证后再购买主域（优先 .com），连续 3–7 天进行中国大陆三网实测；若大陆体验是硬指标且不达标，再办理 ICP 并迁移 EdgeOne Pages 含大陆区域。形成 outputs/deployment-decision-2026-07-17.md。
- 用户随后明确要求不采用 Cloudflare 永久部署，必须先按“FFmpeg 抽帧 + Whisper 转写 + 视觉识别 + 汇总”理解抖音视频，再严格采用视频推荐的生产级部署方式。已取消 Cloudflare/临时托管任务；公开页面受反爬保护，用 Edge Cookie 重试下载仍返回 403（抖音签名参数校验，普通 Cookie 不足）。注意：之前隔离 Python 环境与 faster-whisper 实际未完成安装（venv 未生成可执行文件）。
- 新约束：安装依赖或环境前必须先询问用户再执行。因抖音无法下载该视频，用户决定后续提供视频的本地录屏文件；收到录屏后用本地 FFmpeg 抽帧/音轨，转写所需的 Whisper 环境安装将先征用户批准。当前等待用户上传录屏，不擅自安装。
- 已收到并分析用户本地录屏（E:/works/个人博客/参考视频/75a471f3e2cf0f3629de43b2b22e9001.mp4）：时长 342.8 秒，分辨率 1702×720。通过 FFmpeg 生成 4 张拼图预览表并直接读图，确认视频真实推荐的部署方式是 **Cloudflare Workers & Pages**（创建项目 → 连接 GitHub/上传静态文件 → 部署 → 可选绑定自定义域名）。与之前用户“不走 Cloudflare”的印象不同，视频本身即使用 Cloudflare Pages 作为生产部署方案。当前等待用户确认是否严格按视频走 Cloudflare 路线，或切换为其他平台。
